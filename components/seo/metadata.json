{
  "collectionName": "components_seo_metadata",
  "info": {
    "name": "metadata",
    "icon": "address-book"
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string",
      "minLength": 5,
      "maxLength": 60,
      "required": true
    },
    "description": {
      "type": "text",
      "minLength": 5,
      "maxLength": 150,
      "required": true
    },
    "image": {
      "model": "file",
      "via": "related",
      "allowedTypes": [
        "images"
      ],
      "plugin": "upload",
      "required": false
    },
    "menu": {
      "model": "menu"
    }
  }
}
